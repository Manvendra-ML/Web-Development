"use strict" // use only new version of javascript.

alert(3+3) // it is not work here because we run our code in node.

// Premetive Data Type ( these data type are immutable)

let name = "username " // It is a string datatype
let age = 19 //Number data type
let something = true // boolean data type

// Important 
let temp = null // it is s special data type which indicates null value of variable in javascript

let college;
// if variable is not assign with any value then its data type is "undefined"